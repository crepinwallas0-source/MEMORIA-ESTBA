import { AlertCircle, Check, ClipboardList, FileText, LayoutDashboard, Loader2, Plus, ShieldCheck, Users, X } from "lucide-react";
import { useEffect, useState } from "react";
import AppShell from "@/components/AppShell";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Input } from "@/components/ui/input";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { getCatalog, getSignedResourceUrl, getStats, listPendingResources, publishResource, rejectResource, supabase, type Catalog } from "@/lib/supabase";
import { toast } from "sonner";

export default function Admin() {
  const { user, isAuthenticated, loading } = useAuth(); const [catalog, setCatalog] = useState<Catalog>(); const [pending, setPending] = useState<any[]>([]); const [stats, setStats] = useState({ published: 0, pending: 0, reports: 0 }); const [busy, setBusy] = useState(false); const [programId, setProgramId] = useState<number>(); const [levelId, setLevelId] = useState<number>(); const [academicYearId, setAcademicYearId] = useState<number>(); const [courseName, setCourseName] = useState(""); const [courseCode, setCourseCode] = useState("");
  const refresh = async () => { const [cat, rows, stat] = await Promise.all([getCatalog(), listPendingResources(), getStats()]); setCatalog(cat); setPending(rows); setStats(stat); setProgramId(value => value ?? cat.programs[0]?.id); setLevelId(value => value ?? cat.levels[0]?.id); setAcademicYearId(value => value ?? cat.academicYears[0]?.id); };
  useEffect(() => { if (isAuthenticated && user?.role === "admin") void refresh().catch(error => toast.error(error.message)); }, [isAuthenticated, user?.role]);
  if (loading) return <AppShell><div className="container py-24 text-center text-[#6c8177]">Chargement de votre espace…</div></AppShell>;
  if (!isAuthenticated) return <AppShell><Gate title="Administration protégée" message="Connectez-vous avec votre compte administrateur." action="Se connecter" onClick={startLogin} /></AppShell>;
  if (user?.role !== "admin") return <AppShell><Gate title="Accès restreint" message="Cette section est réservée à l'administrateur de MEMORIA." action="Retour à la bibliothèque" onClick={() => { window.location.href = "/bibliotheque"; }} /></AppShell>;
  const addCourse = async (event: React.FormEvent) => { event.preventDefault(); const institutionId = catalog?.institutions[0]?.id; if (!institutionId || !programId || !levelId || !academicYearId || !courseName.trim()) { toast.error("Complétez le nom de l'UE et son contexte"); return; } setBusy(true); const { error } = await supabase.from("courses").insert({ institution_id: institutionId, program_id: programId, level_id: levelId, academic_year_id: academicYearId, code: courseCode.trim() || null, name: courseName.trim() }); setBusy(false); if (error) toast.error(error.message); else { toast.success("UE ajoutée au référentiel"); setCourseName(""); setCourseCode(""); void refresh(); } };
  const inspect = async (path?: string) => { if (!path) return; try { window.open(await getSignedResourceUrl(path), "_blank", "noopener,noreferrer"); } catch { toast.error("Impossible d’ouvrir le document"); } };
  const decide = async (resource: any, status: "published" | "rejected") => { setBusy(true); try { if (status === "published") await publishResource(resource.id, resource.filePath); else { const reason = window.prompt("Motif du rejet"); if (!reason) return; await rejectResource(resource.id, reason); } toast.success("Décision enregistrée"); await refresh(); } catch (error) { toast.error(error instanceof Error ? error.message : "Impossible d’enregistrer la décision"); } finally { setBusy(false); } };
  return <AppShell><section className="border-b border-[#dce8df] bg-[#f8f0e2]"><div className="container py-10"><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[0.18em] text-[#8b5319]"><LayoutDashboard size={15} />Centre de pilotage</div><h1 className="mt-2 font-display text-4xl font-bold tracking-tight text-[#0f4c45]">Administration</h1><p className="mt-3 text-sm text-[#6c8177]">Traitez les contributions et maintenez le référentiel officiel.</p></div></section><section className="container py-10"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><StatCard label="En attente" value={stats.pending} icon={<ClipboardList size={18} />} accent="amber" /><StatCard label="Publiées" value={stats.published} icon={<Check size={18} />} accent="green" /><StatCard label="Signalements ouverts" value={stats.reports} icon={<AlertCircle size={18} />} accent="red" /><StatCard label="Filières" value={catalog?.programs.length ?? 4} icon={<Users size={18} />} accent="purple" /></div><div className="mt-8 rounded-2xl border border-[#dce8df] bg-white p-5"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-[#e0f0e5] text-[#0f4c45]"><Plus size={17} /></div><div><h2 className="font-display text-lg font-bold text-[#0f4c45]">Renseigner une UE officielle</h2><p className="text-xs text-[#7b9288]">Ne créez que les appellations confirmées par l’établissement.</p></div></div><form onSubmit={addCourse} className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><Input value={courseCode} onChange={e => setCourseCode(e.target.value)} placeholder="Code (facultatif)" className="border-[#dce8df]" /><Input required value={courseName} onChange={e => setCourseName(e.target.value)} placeholder="Nom officiel de l’UE" className="border-[#dce8df] lg:col-span-2" /><Select label="Filière" value={programId} onChange={setProgramId} options={catalog?.programs.map(p => ({ id: p.id, label: p.code })) ?? []} /><Select label="Niveau" value={levelId} onChange={setLevelId} options={catalog?.levels.map(p => ({ id: p.id, label: p.label })) ?? []} /><Select label="Année" value={academicYearId} onChange={setAcademicYearId} options={catalog?.academicYears.map(p => ({ id: p.id, label: p.label })) ?? []} /><Button disabled={busy} type="submit" className="rounded-full bg-[#0f4c45] text-white hover:bg-[#0a3b36] sm:col-span-2 lg:col-span-1"><Plus size={15} />Ajouter</Button></form></div><div className="mt-10 flex items-center justify-between"><div><div className="text-xs font-bold uppercase tracking-[0.18em] text-[#c87536]">À traiter</div><h2 className="mt-1 font-display text-2xl font-bold text-[#0f4c45]">Ressources en attente</h2></div><Badge className="border-0 bg-[#f8e6c8] text-[#8b5319]">{pending.length} dossiers</Badge></div><div className="mt-5 space-y-3">{pending.length === 0 && <div className="rounded-2xl border border-dashed border-[#c6d9ca] bg-white/70 p-14 text-center"><div className="mx-auto flex h-12 w-12 items-center justify-center rounded-2xl bg-[#e0f0e5] text-[#0f4c45]"><ShieldCheck size={22} /></div><h3 className="mt-4 font-display text-xl font-bold text-[#0f4c45]">Tout est à jour</h3><p className="mt-2 text-sm text-[#6c8177]">Aucune ressource n'attend votre décision.</p></div>}{pending.map(resource => <article key={resource.id} className="rounded-2xl border border-[#dce8df] bg-white p-5 shadow-sm"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-center"><div className="flex min-w-0 items-start gap-3"><div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-[#f8e6c8] text-[#8b5319]"><FileText size={20} /></div><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><Badge className="border-0 bg-[#fff5e8] text-[#8b5319]">{resource.resource_type}</Badge><span className="text-xs text-[#9aada3]">Soumis le {new Date(resource.created_at).toLocaleDateString("fr-FR")}</span></div><h3 className="mt-1 truncate font-display text-lg font-bold text-[#18332e]">{resource.title}</h3><div className="mt-1 flex flex-wrap gap-2 text-xs font-semibold text-[#6c8177]"><span>{resource.programCode}</span><span>·</span><span>{resource.levelLabel}</span><span>·</span><span>{resource.academicYearLabel}</span><span>·</span><span>{resource.courseName}</span></div></div></div><div className="flex shrink-0 flex-wrap gap-2"><Button disabled={busy} onClick={() => void inspect(resource.filePath)} variant="outline" className="h-10 rounded-full border-[#cfe0d5] text-[#0f4c45]"><FileText size={15} />Inspecter</Button><Button disabled={busy} onClick={() => void decide(resource, "published")} className="h-10 rounded-full bg-[#0f4c45] text-white hover:bg-[#0a3b36]"><Check size={15} />Publier</Button><Button disabled={busy} onClick={() => void decide(resource, "rejected")} variant="outline" className="h-10 rounded-full border-[#e7c6bd] text-[#a94c38]"><X size={15} />Rejeter</Button></div></div></article>)}</div></section></AppShell>;
}
function Gate({ title, message, action, onClick }: { title: string; message: string; action: string; onClick: () => void }) { return <div className="container py-24"><div className="mx-auto max-w-lg rounded-[2rem] border border-[#dce8df] bg-white p-8 text-center shadow-xl shadow-[#18332e]/5"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-[#f8e6c8] text-[#8b5319]"><ShieldCheck size={25} /></div><h1 className="mt-5 font-display text-3xl font-bold text-[#0f4c45]">{title}</h1><p className="mt-3 text-sm leading-6 text-[#6c8177]">{message}</p><Button onClick={onClick} className="mt-6 rounded-full bg-[#0f4c45] text-white hover:bg-[#0a3b36]">{action}</Button></div></div>; }
function StatCard({ label, value, icon, accent }: { label: string; value: number; icon: React.ReactNode; accent: "amber" | "green" | "purple" | "red" }) { const styles = { amber: "bg-[#fff3df] text-[#9b611d]", green: "bg-[#e0f0e5] text-[#0f4c45]", purple: "bg-[#eae5f5] text-[#654e91]", red: "bg-[#fae5e0] text-[#a94c38]" }; return <div className="rounded-2xl border border-[#dce8df] bg-white p-5"><div className={`flex h-9 w-9 items-center justify-center rounded-xl ${styles[accent]}`}>{icon}</div><div className="mt-4 font-display text-3xl font-bold text-[#18332e]">{value}</div><div className="mt-1 text-xs font-semibold text-[#7b9288]">{label}</div></div>; }
function Select({ label, value, onChange, options }: { label: string; value?: number; onChange: (value: number | undefined) => void; options: { id: number; label: string }[] }) { return <label className="text-xs font-semibold text-[#5d746a]">{label}<select required value={value ?? ""} onChange={e => onChange(e.target.value ? Number(e.target.value) : undefined)} className="mt-1 h-10 w-full rounded-md border border-[#dce8df] bg-white px-2 text-sm font-normal"><option value="">Choisir</option>{options.map(option => <option key={option.id} value={option.id}>{option.label}</option>)}</select></label>; }
